* SSG Miscellanea
- [ ] Sitemap
- [ ] RSS Feed
- [x] Use frontmatter
  - [x] Retrieve from Pandoc using data#frontmatter selection
- [x] Post list on index page
- [x] Syntax highlighting
  - [x] Consider https://fasterthanli.me/articles/introducing-arborium ?
  - [x] Syntax highlighting theme selector (Arborium)
- [x] Heading self-permalink affix
- [x] Theme selector (light/dark)
- [x] Tags
  - [x] Add to post template
  - [x] Make tag collection pages
  - [x] Make tag list (with counters)
  - [x] Add to posts

* Quality of Life
- [ ] Figure out a nicer way to handle paths
  - [ ] Replace output router with something that isn't so delirious, same for resource router
- [x] Web server
- [ ] Hot reloading with livereload websocket (self-refer to Gleam project)
- [ ] Auto-regeneration with hash based checking for incremental compilation connected up to file watcher and hot reloading
- [ ] Automatic archive.is for all external content
- [x] Append and prepend hr#note to the content of all aside

* Optimization
- [ ] Incremental compilation
  - [x] Keep database of hashes of inputs
  - [ ] Only regenerate output if input hash changes

* Thumbnailing
- [x] Build a list of local image files in traversal of the file structure
- [ ] When processing links within the markdown, check if they are within the list of images
- [ ] When that is the case, transform them into the thumbnail & link

- [x] Move away from transforming a>img via changing src of the img to .thumb
- [x] Change markdown documents to omit the link wrap around the images
- [ ] Automatically create links for all img src= where the path is within the local filesystem

* Compilation pipelining
- [x] Be preprocessor independent, support pandoc, asciidoc, commonmark, ...
- [x] Figure out pandoc / preprocessor -> template -> output pipelining

* HTML / Templates

** Huff (Hiccup)
- [X] Write template functions for header, footer, main
- [x] Write template function for post embedding

** Lucy (Emmet-like language for HTML)
- [ ] Look into using either interparse or jasentaa for parsing
- [ ] Formalize Lucy's grammar
- [ ] Look into embedded Clojure expressions within Lucy
- [ ] Avoid actual HTML render effort by converting to Huff/Hiccup under the hood?
- [ ] Figure out lucy -> clojure -> lucy expression, e.g. for loops over variables, ...

* CSS DSL / preprocessing
- [ ] Provide a decent way to do cartesian product mapping with CSS variable interpolation on top of a file demanding variables for attribute definitions, then allow in-line inclusions to produce javascript-less arborium theme management based upon :checked and [value=] selection.
